export const selectIsOpenModal = (state: any) => state.chat.isModal;
export const selectIsFullScreen = (state: any) => state.chat.fullScreen;
export const selectIsWritingMessage = (state: any) => state.chat.writingMessage;
